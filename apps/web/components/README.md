# apps/web/components

`ui/` — shadcn/ui, wrappers RTL-aware. `domain/` — composants métier (VoteCard, ChargeSummaryCard,
StatusBadge, NotificationBanner, EmptyState, ConfirmDialog...) listés en Master Spec Partie 14.3.
